export const galleryPageContent = {
  eyebrow: 'Site Gallery',
  title: 'From foundation to facade.',
  description:
    'A behind-the-scenes look at the work that goes into every OD Construction project \u2014 filter by category or status.',
};

/**
 * Images themselves are not duplicated here — they're generated automatically
 * from every project in /data/projects.ts (see src/lib/media.ts), so a photo
 * only ever needs to exist in one place: the project's own media folder.
 */
